# ========================================================
# 小挑 (Xiaotiao) — Nginx 站点配置
# ========================================================
# 使用方法:
#   1. 复制到 /etc/nginx/sites-available/xiaotiao.conf
#   2. ln -s /etc/nginx/sites-available/xiaotiao.conf /etc/nginx/sites-enabled/
#   3. 删除默认配置: rm /etc/nginx/sites-enabled/default
#   4. 修改 YOUR_DOMAIN_OR_IP 为实际域名或公网 IP
#   5. 修改 /home/deploy/xiaotiao-main 为实际项目路径
#   6. nginx -t && systemctl reload nginx
# ========================================================

# --- HTTP 配置 ---
server {
    listen 80;
    server_name YOUR_DOMAIN_OR_IP;

    # 前端静态文件
    root /home/deploy/xiaotiao-main/xiaotiao-app/dist;
    index index.html;

    # 客户端上传大小限制 (论文 PDF)
    client_max_body_size 50M;

    # Gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml;
    gzip_min_length 1024;

    # ---- 后端 API 反向代理 ----
    # 匹配所有后端路由前缀
    location ~ ^/(auth|topic|article|translation|vocab|research|papers|tracker|collections|multimodal|health|docs|openapi\.json|redoc) {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # LLM 请求可能耗时较长
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
    }

    # ---- 前端 SPA 回退 ----
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}

# --- HTTPS 配置 (取消注释以启用) ---
# 使用 certbot 申请证书:
#   sudo apt install certbot python3-certbot-nginx
#   sudo certbot --nginx -d YOUR_DOMAIN
#
# server {
#     listen 443 ssl http2;
#     server_name YOUR_DOMAIN;
#
#     ssl_certificate /etc/letsencrypt/live/YOUR_DOMAIN/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/YOUR_DOMAIN/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     # (以下内容与 HTTP 版本中 root ~ location 部分相同)
#     root /home/deploy/xiaotiao-main/xiaotiao-app/dist;
#     index index.html;
#     client_max_body_size 50M;
#
#     location ~ ^/(auth|topic|article|translation|vocab|research|papers|tracker|collections|multimodal|health|docs|openapi\.json|redoc) {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 120s;
#         proxy_send_timeout 120s;
#     }
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
#         expires 7d;
#         add_header Cache-Control "public, immutable";
#     }
# }
#
# # HTTP → HTTPS 重定向
# server {
#     listen 80;
#     server_name YOUR_DOMAIN;
#     return 301 https://$host$request_uri;
# }
